# Personal Website

Website built using Vite, React + Typescript + TailwindCSS.

## Pre-requisites

You should have node.js installed if you want to develop locally.

## Development

1. Install the necessary dependencies:

```bash
npm install
```

2. Run the development server:

```bash
npm run dev
```
